import React from 'react'

const ResultsPage = () => {
  const [game, setgame] = useState(null)

  useEffect(async () => {
    await requests
  }, [])

  return (
    <div>ResultsPage</div>
  )
}

export default ResultsPage